import React from 'react';
import { } from 'react-router-dom';

const Header = () => {


    return (
        <header className="app-header">
            <h1 style={{ margin: '0 auto', textAlign: 'center', width: '100%' }}>Portfolio Manager</h1>
        </header>
    );
};

export default Header;
